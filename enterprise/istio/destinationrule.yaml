apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: qa-dashboard-mtls
  labels:
    app.kubernetes.io/name: qa-dashboard
    app.kubernetes.io/component: istio
spec:
  host: qa-dashboard.qa-monitoring.svc.cluster.local
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL
    loadBalancer:
      simple: ROUND_ROBIN
    connectionPool:
      tcp:
        maxConnections: 100
        connectTimeout: 30s
      http:
        http1MaxPendingRequests: 10
        maxRequestsPerConnection: 10
        maxRetries: 3
    outlierDetection:
      consecutive5xxErrors: 3
      interval: 10s
      baseEjectionTime: 30s
      maxEjectionPercent: 50

---
# DestinationRule for Prometheus communication
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: prometheus-mtls
  labels:
    app.kubernetes.io/name: qa-dashboard
    app.kubernetes.io/component: istio
spec:
  host: prometheus-k8s.monitoring.svc.cluster.local
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL
    loadBalancer:
      simple: ROUND_ROBIN

---
# DestinationRule for Grafana communication
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: grafana-mtls
  labels:
    app.kubernetes.io/name: qa-dashboard
    app.kubernetes.io/component: istio
spec:
  host: grafana.monitoring.svc.cluster.local
  trafficPolicy:
    tls:
      mode: ISTIO_MUTUAL
    loadBalancer:
      simple: ROUND_ROBIN